{
  "name": "@atlas/desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:nova-local": "tauri dev --config src-tauri/tauri.dev.conf.json",
    "bundle": "tauri build",
    "icons": "node scripts/generate-icons.mjs",
    "speech": "node scripts/fetch-speech.mjs",
    "voices": "node scripts/fetch-speech.mjs",
    "check": "cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "^2.7.3"
  }
}
