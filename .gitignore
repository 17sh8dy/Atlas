node_modules/
dist/
.turbo/
.vite/
*.log
.env
.env.*
!.env.example
!apps/web/.env.development.example
.DS_Store
Thumbs.db

# Rust / Tauri build output
target/
apps/desktop/src-tauri/gen/

# Speech engine and voice model — fetched, never committed.
# ~114 MB of binaries and neural weights; a repo is for source. Reproduce with
#   pnpm --filter @atlas/desktop voices
# which downloads, verifies the exact byte sizes, and unpacks into place.
apps/desktop/src-tauri/vendor/

# Local build output copied next to the desktop app (27 MB binary)
apps/desktop/Atlas.exe

# The installer was pointed at this folder once: keep its output out of the repo.
/atlas-desktop.exe
/uninstall.exe
/vendor/
