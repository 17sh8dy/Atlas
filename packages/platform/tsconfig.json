{
  "extends": "@atlas/config/tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    // This package is the only one that touches browser globals — it is the
    // platform layer, so DOM types belong here and nowhere below it.
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src"]
}
